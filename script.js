const input = document.getElementsByClassName('largeIcon');

function hoverSize() {
    if (img.height === 300px) {
        img.height = 400px;
        img.width = 400px;
    }
}

largeIcon.addEventListener('mouseover', hoverSize)
